version: '3.9'

services:
  frontend:
    image: frontend
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      $PORT: 433
    ports:
      - 433:433
  api: 
    image: api
    build: 
      context: ./backend
      dockerfile: ./backend/Dockerfile
    environment:

